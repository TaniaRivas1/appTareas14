array.forEach(function (element) {
    
});

array.forEach(element,index => {
    console.log(index);
});
// son lo mismo tanto como  si el callback fuera una funcion o no. Es lo mismo que una funcion.